global:
  scrape_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: 'sustainage_app'
    static_configs:
      - targets: ['host.docker.internal:8000', 'web:8000']
    metrics_path: /metrics
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
